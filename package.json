{
  "name": "messager",
  "version": "0.0.1",
  "description": "Gère le MQ.",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "cross-env NODE_ENV=test mocha --recursive --require @babel/register --exit \"test/**.js\"",
    "build": "rimraf dist && yarn run test && babel src -d dist",
    "prepublishOnly": "npm version patch && npm run build",
    "npmpublish": "npm publish"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "0.5.2",
    "lodash": "4.17.5"
  },
  "devDependencies": {
    "@babel/cli": "7.2.0",
    "@babel/core": "7.2.0",
    "@babel/node": "7.2.0",
    "@babel/plugin-transform-runtime": "7.2.0",
    "@babel/preset-env": "7.2.0",
    "@babel/runtime": "7.2.0",
    "chai": "4.1.2",
    "cross-env": "5.2.0",
    "mocha": "5.0.3",
    "nodemon": "1.18.8",
    "rimraf": "2.6.2"
  }
}
